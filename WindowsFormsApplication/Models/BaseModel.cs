namespace Models
{
    public class BaseModel
    {
    }

    public enum ValidUnit {
        NONE,
        YEAR,
        MONTH,
        DAY
    }

    public enum Status
    {
        Enabled,
        Disabled
    }
}
